#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "Running pre-commit checks..."

# Run lint-staged
npx lint-staged

# If lint-staged fails, exit with error
if [ $? -ne 0 ]; then
  echo "Pre-commit checks failed. Please fix the issues above."
  exit 1
fi

echo "Pre-commit checks passed!"
